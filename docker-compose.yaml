version: "3.9"

services:
  api:
    build: .
    container_name: mirav_perfumes_api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    networks:
      - backend

networks:
  backend:
    external: true